# This machine has ~7 GB of RAM: an 8 GB Gradle heap makes the daemon (and R8)
# die mid-build instead of building. 2 GB is enough for this project.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# All dependencies are AndroidX already — jetifier only costs build time/memory.
android.enableJetifier=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
